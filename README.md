## Coding Exam
> 2.Please provide explanation on why your UI and UX implementation and code implementation is the best. And write it in readme.md and include it in your repository.

- UI
    - Color scheme is Black and White
    - Kept simple by just having buttons to change the location
- UX
    - Black and white scheme was chosen in consideration the end user being outside
    - High contrast colors are the best for visibility when outside
    - Only having the information that the user needs or wants to see
    - Added a link below the venue/place to redirect user to Google Maps
